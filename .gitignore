# ── Dependencies / build output ──
node_modules/
.next/
out/
build/
__pycache__/
*.pyc
.venv/
venv/

# ── Secrets / local env ──
.env
.env.*
!.env.example
!.env.local.example
Chatbot keys.txt
backend/data/embeddings.partial.npy
# incremental-embedding vector cache / checkpoint (local only; the committed
# pair is chunks_merged.jsonl + embeddings.npy)
backend/data/embeddings_cache.npz
backend/data/embeddings_cache.npz.tmp.npz
# eval candidates are unreviewed flash-lite drafts, not ground truth
ingest/eval/eval_candidates.jsonl
# the eval ledger is a local record of runs on this machine (each row is one
# run's four metrics + its k and context size), not a shared artifact
ingest/eval/results.tsv

# ── Editor / OS / local agent settings ──
.claude/
.vercel/
.DS_Store
*.tsbuildinfo

# ── Excluded from this app-only repo (large/source material) ──
Documents/
gradio/
presentation/
*.pptx
